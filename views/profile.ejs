<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
  <link rel="stylesheet" href="../assets/bulma.min.css" type="text/css">
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="../assets/style.css">
  <script></script>
  <title>CONTACT-GROUPS</title>
</head>
<body>

  <% include assets/partials/top-nav %>

  <div class="container" >
    <h4 class="title is-3" style="text-align:center;">EXPRESS CONTACTS - GROUPS</h4>
    <p class="subtitle is-4" style="text-align:center;">all <strong>Profile</strong> information</p>
    <table class="table is-bordered is-striped is-narrow is-fullwidth">
    <thead>

        <tr>
          <th><abbr title="id"></abbr>ID</th>
          <th><abbr title="password">CONTACT</abbr></th>
          <th>USER NAME</th>
          <th><abbr title="password">PASSWORD</abbr></th>
          <th>ACTIONS</th>
        </tr>

    </thead>
    <tbody>
      <%for(let i=0; i<data_Profile.length; i++){%>
      <tr>
        <th><%=data_Profile[i].id%></th>
        <td><strong><%=data_Profile[i].name%></strong></td>
        <td><%=data_Profile[i].username%></td>
        <td><%=data_Profile[i].password%></td>
        <td style="margin-left:-5px;">
              <a class="button is-outlined is-small" href='/profile/edit/<%=data_Profile[i].id%>'>
              <span class="icon is-small">
                <i class="fa fa-pencil"></i>
              </span>
              <span>Edit</span>
              </a>
              <a onclick="myFunction()" class="button is-outlined is-small" href="/profile/delete/<%=data_Profile[i].id%>">
              <span class="icon is-small">
                <i class="fa fa-times"></i>
              </span>
              <span>Delete</span>
              </a>
          </td>
      </tr>
      <%}%>
    </tbody>
  </table>
  <hr>
  <form action="/profile" method="post" onSubmit="validasiProfile()">
    <div class="field-body">
      <div class="field">
        <div class="control">
          <input class="input" id="namauser" type="text" placeholder="username" name="username" required>
        </div>
      </div>
      <div class="field">
        <div class="control">
          <input class="input" id="passuser" type="text" placeholder="password" name="password" required>
        </div>
      </div>
      <div class="field">
        <p class="control has-icons-left">
          <span class="select">
            <select id="selectuser" name="name">
                    <% for(let i=0; i<data_Contacts.length; i++) {%>
                    <option value="<%= data_Contacts[i].id %>"><%= data_Contacts[i].name %></option>
                    <%}%>
            </select>
          </span>
          <span class="icon is-small is-left">
            <i class="fa fa-user"></i>
          </span>
        </p>
      </div>
      <div class="control">
        <button type="submit" class="button is-success">Submit</button>
      </div>
    </div>
  </form>
  <div style="text-align:center; margin-top:13px;">
    <a style="color:red; text-decoration: none; font-weight:bold;" href="#"><%=pesanError%></a>
  </div>
</div>

  </div>


<script src="assets/function.js"></script>
</body>
</html>
